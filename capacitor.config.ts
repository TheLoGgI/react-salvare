import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.salavare.app',
  appName: 'Salvare',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
